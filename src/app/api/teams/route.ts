import { NextResponse } from "next/server";

// Placeholder for teams API routes
export async function GET() {
  return NextResponse.json(
    { message: "Teams API - Coming soon" },
    { status: 501 }
  );
}

export async function POST() {
  return NextResponse.json(
    { message: "Teams API - Coming soon" },
    { status: 501 }
  );
}

