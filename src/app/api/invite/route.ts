import { NextResponse } from "next/server";

// Placeholder for invite API routes
export async function POST() {
  return NextResponse.json(
    { message: "Invite API - Coming soon" },
    { status: 501 }
  );
}

export async function GET() {
  return NextResponse.json(
    { message: "Invite API - Coming soon" },
    { status: 501 }
  );
}

