import { NextResponse } from "next/server";

// Placeholder for activity log API routes
export async function GET() {
  return NextResponse.json(
    { message: "Activity API - Coming soon" },
    { status: 501 }
  );
}

