import { NextResponse } from "next/server";

// Placeholder for organization API routes
export async function GET() {
  return NextResponse.json(
    { message: "Organization API - Coming soon" },
    { status: 501 }
  );
}

export async function PATCH() {
  return NextResponse.json(
    { message: "Organization API - Coming soon" },
    { status: 501 }
  );
}

