import { NextResponse } from "next/server";

// Placeholder for quotas API routes
export async function GET() {
  return NextResponse.json(
    { message: "Quotas API - Coming soon" },
    { status: 501 }
  );
}

