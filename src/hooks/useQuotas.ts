"use client";

// Placeholder hook for resource quotas
export function useQuotas() {
  // TODO: Implement quotas data fetching with Tanstack Query
  return {
    quotas: null,
    usage: null,
    loading: true,
    error: null,
  };
}

